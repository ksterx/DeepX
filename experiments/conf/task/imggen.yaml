name: imggen

model: dcgan
batch_size: 32
lr: 1e-4
beta1: 0.5
beta2: 0.999
negative_slope: 0.2
dropout: 0.1
latent_dim: 100
base_dim_g: 128
base_dim_d: 128
loss_fn: bce
optimizer: adam
scheduler: null
monitor: val_loss_g
monitor_max: false

defaults:
  - base
